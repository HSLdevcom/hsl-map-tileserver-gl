name: Build

on:
  pull_request:
    branches-ignore:
      - 'main'

jobs:
  build-container:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          tags: hsldevcom/hsl-map-tileserver-gl:dev
          build-args: |
            BUILD_ENV=dev
            DIGITRANSIT_APIKEY=${{ secrets.KARTAT_DIGITRANSITAPIKEY_DEV }}